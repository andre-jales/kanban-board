import styled from "@emotion/styled";

import { Language } from "../../../shared/components";

export const SelectLanguageContainer = styled.div({
  marginLeft: "auto",
});

export const StyledLanguage = styled(Language)({
  color: "#fff",
});
